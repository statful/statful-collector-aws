import expect from 'expect';
import Collector from '../../src/collector';

describe('Module', () => {
    describe('#getModule()', () => {
        it('should find module', () => {
            expect(true).toExist();
        });
    });
});
