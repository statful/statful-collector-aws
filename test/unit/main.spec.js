import expect from 'expect';
//import { classModule } from '../../src';

describe('Module', () => {
    describe('#getModule()', () => {
        it('should find module', () => {
            expect(true).toExist();
        });
    });
});